# Example environment variables for docker/dev compose
# Copy this file to .env or export the variables before running docker-compose

# NOTE: Docker Compose will substitute ${VAR} when starting the container.
# Spark will see these variables at driver runtime (inside the submitter container).
# Executors do not automatically inherit the container env; the compose service
# uses spark-submit flags to propagate important vars to executors via
# --conf spark.executorEnv.VAR_NAME. Keep these values here and adjust as needed.

# Kafka bootstrap servers reachable from the spark container
KAFKA_BOOTSTRAP_SERVERS=kafka:9092

# Kafka topics to subscribe to (supports regex when using subscribePattern)
KAFKA_SUBSCRIBE_TOPICS=parsed-trades-.*-usdt

# Kafka output topic for VWAP results
KAFKA_OUTPUT_TOPIC=vwap-results

# Spark app name
SPARK_APP_NAME=vwap-spark-app

SPARK_MASTER_URL="spark://spark-master:7077"
